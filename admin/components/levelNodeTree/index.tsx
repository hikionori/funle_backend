import { Box } from "@chakra-ui/react";
import { Content } from "../../utils/admin-sdk";


export interface LevelNodeTreeProps {
    contentLevels: Content[][];
}

export default function LevelNodeTree(props: LevelNodeTreeProps) {
    return (
        <Box></Box>
    );
}